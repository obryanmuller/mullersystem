generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") // O Prisma vai ler a variável do seu arquivo .env
}

model Produto {
  id          Int      @id @default(autoincrement())
  nome        String
  sku         String   @unique
  preco       Decimal  @db.Decimal(10, 2)
  quantidade  Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Cliente {
  id            Int      @id @default(autoincrement())
  nome          String
  email         String   @unique
  telefone      String?
  cpf           String   @unique
  status        String   @default("Ativo")
  totalCompras  Decimal  @default(0) @db.Decimal(10, 2)
  enderecoRua   String
  enderecoBairro String
  enderecoCidade String
  enderecoEstado String
  enderecoRef   String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}